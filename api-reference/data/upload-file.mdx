---
title: "Upload File"
api: "POST https://api.moorcheh.ai/v1/namespaces/{namespace_name}/upload-file"
description: "Directly upload files to a text namespace for semantic search and AI-powered operations."
contentType: "multipart/form-data"
---

## Overview

Upload files directly to a text namespace, enabling semantic search, similarity matching, and AI-powered question answering. The API automatically extracts text content from various file formats including PDF, DOCX, TXT, and more. Files are processed to generate embeddings using Amazon Bedrock for optimal search performance.

<Info>
Files are automatically processed to extract text content and generate embeddings. Supported formats include PDF, DOCX, TXT, MD, CSV, and more.
</Info>

## Authentication

<ParamField header="x-api-key" type="string" required>
  Your API key for authentication
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Must be `multipart/form-data` for file uploads
</ParamField>

## Path Parameters

<ParamField path="namespace_name" type="string" required>
  Name of the text namespace to upload files to
</ParamField>

## Request Body (multipart/form-data)

<Note>
When testing with the "Try it" feature, you can either upload a file directly or specify a file path. In cURL commands, use `-F "file=@/path/to/your/file.pdf"` where `@` indicates the file path on your system.
</Note>

<ParamField body="file" type="file" required>
  The file to upload (max 10MB). Must be one of the allowed file types: .pdf, .docx, .xlsx, .json, .txt, .csv, .md
</ParamField>

## Supported File Types

<CardGroup cols={3}>
  <Card title=".pdf" icon="file-pdf">
    PDF documents
  </Card>
  <Card title=".docx" icon="file-word">
    Microsoft Word documents
  </Card>
  <Card title=".xlsx" icon="file-excel">
    Microsoft Excel spreadsheets
  </Card>
  <Card title=".json" icon="brackets-curly">
    JSON data files
  </Card>
  <Card title=".txt" icon="file-lines">
    Plain text files
  </Card>
  <Card title=".csv" icon="table">
    CSV data files
  </Card>
  <Card title=".md" icon="markdown">
    Markdown files
  </Card>
</CardGroup>

<RequestExample>

```bash Single File Upload
# Replace 'my-documents' with your namespace name
# Replace 'your-api-key-here' with your actual API key
# Replace '/path/to/document.pdf' with the actual path to your file

curl -X POST "https://api.moorcheh.ai/v1/namespaces/my-documents/upload-file" \
  -H "x-api-key: your-api-key-here" \
  -F "file=@/path/to/document.pdf"
```

```bash Multiple Files Upload
# Upload multiple files by sending the 'file' field multiple times
curl -X POST "https://api.moorcheh.ai/v1/namespaces/technical-docs/upload-file" \
  -H "x-api-key: your-api-key-here" \
  -F "file=@/path/to/doc1.pdf"
```

```javascript JavaScript Example
const namespaceName = 'my-documents';
const formData = new FormData();
formData.append('file', fileInput.files[0]);

const response = await fetch(`https://api.moorcheh.ai/v1/namespaces/${namespaceName}/upload-file`, {
  method: 'POST',
  headers: {
    'x-api-key': 'your-api-key-here'
  },
  body: formData
});

const result = await response.json();
console.log(result);
```

```python Python Example
import requests

namespace_name = 'my-documents'
url = f"https://api.moorcheh.ai/v1/namespaces/{namespace_name}/upload-file"
headers = {
    "x-api-key": "your-api-key-here"
}

files = [
    ('file', ('document.pdf', open('document.pdf', 'rb'), 'application/pdf'))
]

response = requests.post(url, headers=headers, files=files)
print(response.json())
```

</RequestExample>

<ResponseExample>

```json 200 - OK
{
  "success": true,
  "message": "File uploaded successfully",
  "namespace": "my-namespace",
  "fileName": "document.pdf",
  "fileSize": 1048576
}
```

```json 400 - Bad Request (No File)
{
  "error": "No file was uploaded",
  "hint": "Make sure to send file as multipart/form-data with field name \"file\""
}
```

```json 400 - Bad Request (Unsupported File Type)
{
  "error": "File type '.exe' is not supported. Allowed types: .pdf, .docx, .xlsx, .json, .txt, .csv, .md",
  "allowedTypes": ["PDF", "DOCX", "XLSX", "JSON", "TXT", "CSV", "MD"]
}
```

```json 400 - Bad Request (File Too Large)
{
  "error": "File size (12.50MB) exceeds maximum allowed size of 10MB"
}
```

```json 400 - Bad Request (Wrong Namespace Type)
{
  "error": "Namespace 'my-vector-namespace' is not text, please use text based namespace"
}
```

```json 401 - Unauthorized
{
  "error": "Unauthorized: API key is required"
}
```

```json 404 - Not Found
{
  "error": "Namespace 'example-namespace' not found for this user"
}
```

```json 500 - Internal Server Error
{
  "error": "Internal server error"
}
```

</ResponseExample>

## Response Fields

### Success Response (200 OK)

<ResponseField name="success" type="boolean">
  Always `true` for successful uploads
</ResponseField>

<ResponseField name="message" type="string">
  Human-readable confirmation message
</ResponseField>

<ResponseField name="namespace" type="string">
  Name of the namespace the file was uploaded to
</ResponseField>

<ResponseField name="fileName" type="string">
  Original name of the uploaded file
</ResponseField>

<ResponseField name="fileSize" type="integer">
  Size of the uploaded file in bytes
</ResponseField>

## Related Endpoints

- [Upload Text](/api-reference/data/upload-text) - Upload text content directly
- [Search](/api-reference/search/query) - Search uploaded files and documents
- [Get Documents](/api-reference/data/get-documents) - Retrieve document information
- [Delete Data](/api-reference/data/delete) - Remove uploaded files
- [Create Namespace](/api-reference/namespaces/create) - Create text namespaces for uploads

